# Copy to `.env` for local development. Never commit real secrets.

# Optional runtime keys (UI can run without these)
INDIANAPI_API_KEY=
ALPHA_VANTAGE_API_KEY=
NEWSDATA_API_KEY=

# EOD screener reads (Streamlit Cloud uses these)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_BUCKET=quantoracle-artifacts

# Publisher-only (GitHub Actions / local publish). DO NOT set in Streamlit Cloud.
SUPABASE_SERVICE_ROLE_KEY=
GROWW_API_KEY=
GROWW_API_SECRET=

